@model IEnumerable<DemoMVC.Models.Entities.Batch>

@{
    Layout = "_CourseLayout";
    ViewData["Title"] = "Danh sách đợt học của khóa học: " + ViewBag.CurrentCourseName;
}

@await Html.PartialAsync("_PageHeader")

@await Html.PartialAsync("_BatchList", Model)